{
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  },
  "installCommand": "npm install --legacy-peer-deps && npm install -g tailwindcss",
  "buildCommand": "npm run build",
  "framework": "nextjs"
} 